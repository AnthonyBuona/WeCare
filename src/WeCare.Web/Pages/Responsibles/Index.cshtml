@page
@using WeCare.Localization
@using WeCare.Permissions
@using WeCare.Web.Pages.Responsibles
@using Microsoft.AspNetCore.Authorization
@using Microsoft.Extensions.Localization
@model IndexModel
@inject IStringLocalizer<WeCareResource> L
@inject IAuthorizationService AuthorizationService

<abp-card>
    <abp-card-header>
        <abp-row>
            <abp-column size-md="_6">
                <abp-card-title>@L["Responsibles"]</abp-card-title>
            </abp-column>
            <abp-column size-md="_6" class="text-end">
                @* @if (await AuthorizationService.IsGrantedAsync(WeCarePermissions.Responsibles.Create)) *@
                @* { *@
                    <abp-button id="NewResponsibleButton"
                                text="@L["New Responsible"].Value"
                                icon="plus"
                                button-type="Primary" />
                @* } *@
            </abp-column>
        </abp-row>
    </abp-card-header>
    <abp-card-body>
        <abp-table striped-rows="true" id="ResponsiblesTable"></abp-table>
    </abp-card-body>
</abp-card>
@section scripts
{
    <abp-script src="/Pages/Responsibles/index.js" />
}